This package provides the `usdview` application and related Qt-based tooling on
top of `usd-imaging`.

For Python versions earlier than 3.11 it expects `PySide2` and `shiboken2`
version `5.15.2.1`. For Python 3.11 and newer it expects `PySide6` and
`shiboken6` version `6.11.0`.
