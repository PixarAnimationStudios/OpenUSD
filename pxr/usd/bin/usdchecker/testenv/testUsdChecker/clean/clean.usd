#usda 1.0
(
    defaultPrim = "World"
    metersPerUnit = 0.01
    subLayers = [
        @./geom.usdc@
    ]
    upAxis = "Y"
)

def "World" (
    prepend references = @./shading.usda@</World>
)
{
    def Scope "scope"
    {
    }

    def Camera "camera"
    {
    }
}

