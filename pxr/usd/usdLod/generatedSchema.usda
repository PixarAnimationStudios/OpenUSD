#usda 1.0
(
    "WARNING: THIS FILE IS GENERATED BY usdGenSchema.  DO NOT EDIT."
)

class "LODRootAPI" (
    customData = {
        string userDocBrief = "API for configuring Level of Detail (LOD) facilities on a prim."
    }
)
{
    int lod:default:index = 0 (
        customData = {
            string userDocBrief = """The LOD item child to use if none of the heuristics are
        understood by the renderer or if none return a usable LOD."""
        }
    )
    rel lod:heuristics (
        customData = {
            string userDocBrief = """Relationship targeting all the heuristic prims that could be
        used for this particular LOD root."""
        }
    )
}

class "LODOverrideAPI" (
    customData = {
        string userDocBrief = "API for overriding Level of Detail (LOD) choices."
    }
)
{
    float lod:override:index = 0 (
        customData = {
            string userDocBrief = 'The index value to use when lod:override:mode is "indexedLOD".'
        }
    )
    token lod:override:mode = "inherited" (
        allowedTokens = ["inherited", "noOverride", "indexedLOD", "noLOD", "allLOD"]
        customData = {
            string userDocBrief = "How to apply override to LOD decisions."
        }
    )
}

class "LODHeuristic" (
    customData = {
        string userDocBrief = "Base class for LOD Heuristics."
    }
)
{
    uniform token lod:domain (
        customData = {
            string userDocBrief = 'The "domain" of this heuristic.'
        }
    )
}

class LODDistanceHeuristic "LODDistanceHeuristic" (
    customData = {
        string userDocBrief = """This LOD heuristic selects LOD children on the basis of distance
    from the view point to the LOD root."""
    }
)
{
    uniform float[] blendThresholds = [] (
        customData = {
            string userDocBrief = """This defines distance thresholds for LOD transitions in
        ascending order."""
        }
    )
    rel boundingVolume (
        customData = {
            string userDocBrief = """Optional relationship to a Boundable prim that defines the
        bounding volume for this LOD Root, the prim's extent will be used for
        distance calculation if it is valid (not empty)."""
        }
    )
    point3f center = (0, 0, 0) (
        customData = {
            string userDocBrief = "The center point of this LOD Root in local coordinates."
        }
    )
    uniform token lod:domain (
        customData = {
            string userDocBrief = 'The "domain" of this heuristic.'
        }
    )
    uniform float[] thresholds = [] (
        customData = {
            string userDocBrief = """This defines the distance thresholds for LOD transitions in
        ascending order."""
        }
    )
}

class LODScreenSizeHeuristic "LODScreenSizeHeuristic" (
    customData = {
        string userDocBrief = """This LOD heuristic selects LOD children on the basis of the
    fraction of the viewplane would be covered by the extent."""
    }
)
{
    uniform float[] blendThresholds = [] (
        customData = {
            string userDocBrief = """This defines screen size thresholds for LOD transitions in
        descending order."""
        }
    )
    rel boundingVolume (
        customData = {
            string userDocBrief = """Optional relationship to a Boundable prim that defines the
        bounding volume for this LOD Root, used for size calculation if
        the referenced prim has a valid extent."""
        }
    )
    float3[] extent = [(-1, -1, -1), (1, 1, 1)] (
        customData = {
            string userDocBrief = "The extent of this LOD Root in local coordinates."
        }
    )
    uniform token lod:domain (
        customData = {
            string userDocBrief = 'The "domain" of this heuristic.'
        }
    )
    uniform token projectionMethod = "projectedSphere" (
        allowedTokens = ["projectedExtent", "projectedSphere"]
        customData = {
            string userDocBrief = "Defines how screen size is calculated."
        }
    )
    uniform float[] thresholds = [] (
        customData = {
            string userDocBrief = """The screen size thresholds for LOD transitions in descending
        order as a fraction of the viewport size."""
        }
    )
}

