#usda 1.0
(
    framesPerSecond = 24
    framePrecision = 3
)

# Note: This is testing parsing syntax only, not allowable semantics.

def MfScope "TestPrim" (
    relocates = {
        <> : </target>
    }
)
{
}

