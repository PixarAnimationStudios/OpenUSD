#usda 1.0

def "ModelGroup" (
    references = @ref.usda@</ModelGroup>

    clipAssetPaths = [@clip-group.usda@]
    clipPrimPath = "/ModelGroup"
    clipActive = [(0.0, 0)]
)
{
    over "Model" (
        clipAssetPaths = [@clip-model.usda@]
        clipPrimPath = "/Model"
        clipActive = [(0.0, 0)]
    )
    {
    }
}