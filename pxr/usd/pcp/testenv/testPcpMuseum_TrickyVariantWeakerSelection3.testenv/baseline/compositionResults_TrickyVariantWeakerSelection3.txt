Loading @TrickyVariantWeakerSelection3/root.usda@

------------------------------------------------------------------------
Layer Stack:
     root.usda

------------------------------------------------------------------------
Results for composing </Model>

Prim Stack:
    root.usda            /Model
    root.usda            /Model{modelComplexity=full}
    geo.usda             /Model
    geo.usda             /Model{K_hierarchy=full}

Variant Selections:
    {K_hierarchy = full}
    {modelComplexity = full}

