#usda 1.0

def __AnyType__ "Model" (
    append references = @./geo.usda@</Model>
    append variantSets = ["modelComplexity"]
    variants = {
        string modelComplexity = "full"
    }
)
{
    variantSet "modelComplexity" = {
        "full" (
            variants = {
                string K_hierarchy = "full"
            }
        ) {

        }
    }
}

