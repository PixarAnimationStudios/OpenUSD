#usda 1.0
(
   """
   This captures a crash bug that was due to invalid dependencies being
   registered, which was due to failed path translations when composing
   variant selections.
   """
)

def Model "FergusCloak" (
    references = @./FergusCloak_standins.usda@</FergusCloak>
)
{
}
