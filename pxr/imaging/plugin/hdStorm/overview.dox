/*!
\page hdStorm_page_front HdStorm : Real-time Hydra renderer plugin
\if ( PIXAR_MFB_BUILD )
\mainpage  HdStorm : Real-time Hydra renderer plugin
\endif

HdStorm implements a Hydra renderer plugin that wraps the Storm Hydra render
delegate. Storm supports OpenGL, Metal and Vulkan graphics APIs by utilizing the
Hydra graphics interface (Hgi).
This is a thin plugin layer, you will find most rendering functionality 
in the HdSt library.

*/
